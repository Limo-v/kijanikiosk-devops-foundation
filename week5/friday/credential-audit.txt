Week 5 Friday Credential Audit Results (2026-04-23)

1) Jenkinsfile credential scan
Command:
`grep -nEi "password|passwd|token|secret|apikey|api_key|authorization" Jenkinsfile`

Output:
97:                withCredentials([usernamePassword(
100:                    passwordVariable: 'NEXUS_PASS'
104:                        token="$(printf '%s:%s' "$NEXUS_USER" "$NEXUS_PASS" | base64 | tr -d '\n')"
109://172.17.0.1:8081/repository/npm-kijanikiosk/:_auth=${token}

Interpretation:
- Matches are variable names/credential plumbing, not hardcoded secret values.

2) Week 5 history scan for credentials
Command:
`git log -p -- week5 Jenkinsfile | grep -nEi "password|passwd|token|secret|apikey|api_key|authorization"`

Output:
- No matches.

Interpretation:
- No literal secret-like values found in scanned diffs.

3) Friday run log scan for unmasked secrets
Command:
`grep -nEi "password|passwd|token|secret|apikey|api_key|authorization" week5/friday/green-pipeline-run.txt`

Output:
- No matches.

Interpretation:
- Current file contains no leaked secret-like terms.
