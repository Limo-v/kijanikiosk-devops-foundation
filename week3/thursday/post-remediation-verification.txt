# Thursday Post-Remediation Verification

I should run these after all 3 fixes, then paste the real terminal output here (still pending from VM side).

vmstat 2 5
df -h /
du -sh /opt/kijanikiosk/shared/logs/
ss -tlnp | grep 3001
sudo ufw status numbered
curl -s http://localhost:3001/ | head -3
journalctl -u kk-payments -p err --since "5 minutes ago"

What I expect to see:
- `wa` lower than before and blocked processes better
- some disk space recovered after rotation
- only one listener on 3001
- deny 3001 rule removed
- localhost payments response looks normal-ish
- fewer or no fresh errors in journal window
