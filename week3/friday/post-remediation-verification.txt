# Friday Post-Remediation Verification

Run after provisioning and after forced logrotate, then paste full output.

```bash
vmstat 2 5
df -h /
du -sh /opt/kijanikiosk/shared/logs/
ss -tlnp | grep 3001
sudo ufw status numbered
curl -s http://localhost:3001/ | head -3
journalctl -u kk-payments -p err --since "5 minutes ago"
sudo logrotate --force --debug /etc/logrotate.d/kijanikiosk
sudo logrotate --force /etc/logrotate.d/kijanikiosk
ls -la /opt/kijanikiosk/shared/logs/
getfacl /opt/kijanikiosk/shared/logs/
sudo -u kk-api touch /opt/kijanikiosk/shared/logs/test-write.tmp \
  && echo "PASS: kk-api can write after logrotate" \
  || echo "FAIL: kk-api cannot write to shared/logs"
```

Expected:
- low I/O wait compared to incident state,
- proper firewall rules in intended order,
- no fresh payments error spike,
- and PASS on the logrotate write test above.
