{
  "service_name": "{{ service_name }}",
  "service_port": {{ service_port }},
  "environment": "{{ environment }}",
  "status": "configured-by-ansible",
  "updated_at": "{{ ansible_date_time.iso8601 }}"
}
